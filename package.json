{
  "name": "customink-tests",
  "version": "0.1.0",
  "private": true,
  "description": "E2E tests for CustomInk header & footer (staging)",
  "scripts": {
    "test": "playwright test",
    "test:p1": "playwright test --grep @p1",
    "test:p2": "playwright test --grep @p2",
    "test:p3": "playwright test --grep @p3",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "test:flaky": "playwright test --grep @p1 --repeat-each 5 --workers 2",
    "test:prod-smoke": "playwright test --project=prod-smoke",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "check:drift": "tsc scripts/check-doc-test-drift.ts --outDir scripts/.compiled --module nodenext --moduleResolution nodenext --target es2022 --strict --esModuleInterop --skipLibCheck --types node && node scripts/.compiled/check-doc-test-drift.js",
    "check": "npm run typecheck && npm run lint && npm run test:p1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "@typescript-eslint/parser": "^8.59.1",
    "dotenv": "^16.4.5",
    "eslint": "^9.13.0",
    "eslint-plugin-playwright": "^2.0.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
