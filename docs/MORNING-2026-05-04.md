# Wake-up checklist — Roman call day (2026-05-04)

Run these in order. Each step is copy-paste; nothing requires thinking.

## State at end of 2026-05-03 night session

- `storage/auth.json` exists (86 cookies; valid; gitignored)
- Setup project verifies auth state correctly: `npx playwright test --project=setup` → 1 passed
- **LOGOUT now passes** — POM widening (button OR link OR menuitem; in-WC OR page-wide) was the fix. See commits.
- **Order History fails for a NEW reason**: it redirects to `account.staging.customink.com` which throws JS pageErrors "Oops! It looks like you're not logged in", because `storage/auth.json` only has cookies for `www-master.staging.customink.com`. **Cross-domain auth state.**

### Late-night run (04:00 local) — staging too slow

Final anonymous suite run finished 31 failed / 4 skipped / 3 passed. Sample error: `ci-header-prerender` Web Component searchbox didn't appear within 10s on /. Staging is rate-limited / sleepy at night. Earlier 13 pass / 9 fail / 16 skip is the realistic baseline; today's regression is timing-of-day, not code. Re-run at normal hours before drawing conclusions.

### Tomorrow's first concrete fixes

1. **Cross-domain auth** — re-run `npx playwright codegen` and during the manual login also visit `https://account.staging.customink.com/` so the codegen captures cookies for that subdomain too. Or use `--save-storage` after navigating to both domains.
2. **OR allowlist the "not logged in" pageError** in `fixtures/pages.fixture.ts` `monitorPageHealth` — cleaner short-term fix; mark with TODO to revisit once cross-domain auth is solved.
3. After (1) or (2), the 7 dropdown items should pass with their current locators (or with small URL pattern adjustments visible in their respective traces).
4. CART persisted + FAVORITES persisted + header heart icon — same story; fix gated on cross-domain auth.

## 0. Quick situational check (1 min)

```bash
cd /Users/kapusansky/DEV/customink-tests
git log --oneline -5
git status
curl -s -o /dev/null -w "%{http_code}\n" https://www-master.staging.customink.com/
```

If staging returns 200 → continue. If 502 → wait 10 minutes, retry. The fix work below all needs staging up.

## 1. Produce `storage/auth.json` (manual, 3 min)

```bash
npx playwright codegen \
  --save-storage=storage/auth.json \
  https://www-master.staging.customink.com/profiles/users/sign_in
```

A browser window opens. In that window:

1. Email: `juraj.kapusansky@gmail.com`
2. Click "Continue With Email"
3. Open Gmail → click magic link → return to the codegen browser logged in
4. Once you see the homepage with "My Account" in the header, **close the codegen window**.

Verify the file:

```bash
ls -la storage/auth.json && jq '.cookies | length' storage/auth.json
```

Cookie count should be > 0. If 0 → the login didn't persist. Repeat.

## 2. Walk & Watch the still-unobserved DOM (15 min)

Tell Claude: "Open Chrome DevTools MCP and walk these pages, capture real locators into `docs/walk-and-watch/2026-05-03-customink-dom-observations.md`":

- `https://www-master.staging.customink.com/products/t-shirts/4` → click first product card → product detail page → find the heart / "save" affordance, capture: accessible name, pressed-state attribute, anonymous visibility
- `https://www-master.staging.customink.com/products/favorites` (anonymous, fresh browser) → capture exact empty-state copy + any sign-in CTA
- Hover the avatar → click "Create An Account" → capture form fields, submit button name, captcha presence

## 3. Apply Walk & Watch findings (10 min)

In `tests/user-journeys.spec.ts`, replace speculative regexes in:

- FAVORITES describe block (lines ~290–410): heart locator + empty-state copy
- REGISTRATION describe block (lines ~420–540): form field labels + submit button name

Tell Claude: "Apply the Walk & Watch observations from `docs/walk-and-watch/2026-05-03-customink-dom-observations.md` to fix the FAVORITES and REGISTRATION tests."

## 4. Run anonymous suite (3 min)

```bash
npx playwright test tests/user-journeys.spec.ts --project=chromium-desktop --workers=2 --reporter=line 2>&1 | tail -40
```

Expected: 0 failures (skips for auth-gated journeys are fine).

If any failure remains, the trace is at `test-results/<test-name>/`. Inspect the screenshot first.

## 5. Run logged-in suite (3 min)

```bash
npx playwright test tests/user-journeys.spec.ts --project=chromium-desktop-authed --reporter=line 2>&1 | tail -30
```

Expected: setup project verifies auth.json, then 11 logged-in tests run. Some dropdown URL patterns are best-effort guesses — the failures from this run tell you what to tighten.

## 6. Tighten any failing dropdown URL patterns (5 min)

If e.g. "Order History" failed because the URL pattern guess didn't match:

- Open the screenshot in `test-results/...`
- Update the `waitForURL` regex in the corresponding describe to match the real URL
- Re-run that single test: `npx playwright test --grep "Order History"`

## 7. Run skills B + C + D (30–45 min)

Tell Claude:

- "Run check-error-handling skill on `tests/user-journeys.spec.ts`"
- "Run quick-code-scan or review-code skill on the whole branch"
- "Update `docs/superpowers/specs/2026-05-03-...` and `docs/superpowers/plans/2026-05-03-...` to reflect the 21 journeys (current code, not the original 13)"

## 8. Final pre-Roman check (5 min)

```bash
npx playwright test --project=chromium-desktop --workers=2 --reporter=list 2>&1 | tail -50
git log --oneline -10
git diff main...HEAD --stat
```

If green and the diff stat looks reasonable → you're ready.

## 9. Push (when comfortable)

```bash
git push origin showcase/header-footer-tests
```

Open the GitHub PR, write a one-line description: what changed, what's tested, what's gated on auth.json. The README user-journey section is already correct — link to it.

## Reference docs

- Spec: `docs/superpowers/specs/2026-05-03-customink-header-user-journeys-redesign-design.md`
- Plan: `docs/superpowers/plans/2026-05-03-customink-header-user-journeys-redesign.md`
- Walk & Watch: `docs/walk-and-watch/2026-05-03-customink-dom-observations.md`
- README user-state section: `README.md` § "User journey suite"

## Talking points for Roman call

1. **Tests don't test → tests do test now.** Every journey clicks through to a user-perceivable outcome; mutation thought-experiment passes for each.
2. **Two states, two blocks.** Header chrome differs by state; the suite mirrors that with two describe blocks. CART and FAVORITES are tested in both (different bug classes per state).
3. **Auth gate.** Logged-in journeys require `storage/auth.json`; produced via codegen, gitignored. The suite skips with a clear reason when absent.
4. **Documented coverage.** Every journey exists, even auth-gated ones — readers see the full set. Nothing silently missing.
